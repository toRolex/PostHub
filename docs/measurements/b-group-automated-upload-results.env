# B 组：自动化链路端到端实测结果（issue #11，2026-08-10）
# 视频号代表三平台验证通过；小红书/抖音链路默认成功（同构 seam 外推，决策不实测）。
# A 组手动实测数据见 a-group-results.env。

# ── 已验证（视频号「阿七专注野生菌」，cdp_port 9223）──
WECHANNEL_CDP_ATTACH=通过（connect_over_cdp 接管、context 复用、browser.close 中和后 Chrome 存活）
WECHANNEL_UPLOAD_IMMEDIATE=成功（PostHub 自动化链路实测，已发布）
WECHANNEL_UPLOAD_SCHEDULED=成功（platform_time +2h 排期，set_schedule_time_tencent 走通）

# ── 实测解决的接线问题 ──
BASE_DIR_UTILS_STEALTH=必须存在；上游 cookie_auth→set_init_script 读 BASE_DIR/utils/stealth.min.js；已从上游包复制到 daemon/utils/
STORAGE_STATE_SYNC=必须导出 CDP context 登录态到 ~/.posthub/cookies/{id}.json；上游 cookie_auth 用自建 async with async_playwright() 绕过 cdp_attach patch

# ── 附加发现 ──
UPSTREAM_RETURNS_POST_URL=否（RESULT_OK=True 但 post_id/post_url 为空，待后续处理）

# ── 降级 / 保留项 ──
XHS_DOUYIN_UPLOAD_LINK=默认成功（决策不实测）
WINDOWS_REAL_MACHINE=降级为已知风险，正式装机时验证（CDP WS 握手 / singleton / Chrome 版本 vs patchright 1.58.2）
BOUNDARY_VALUES=保留为已知未确认值（抖音 60min vs 15min、视频号 20GB vs 2G、小红书封面/图片/时长、视频号单日条数）
