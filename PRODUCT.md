# Product

## Register

product

## Platform

web

Tauri 2 桌面应用，前端由 WebView 渲染（Windows = WebView2 / Chromium）。impeccable 语义为 `web`；交互与密度按**桌面工具**处理（窗口固定、键盘可操作、无移动端）。

## Users

短视频创作者与自媒体运营者，在 Windows / macOS 桌面上发布与监控。他们的真实工作流：准备一批本地素材（视频 + 文案 + 封面，或 Excel 任务表）→ 在已配置的多平台账号上发布 → 定时/批量任务无人值守跑 → 回来盯任务状态 → 处理人工介入（验证码挂起、需重新扫码）。逐平台手动发布耗时、易错、易触发风控，这是 PostHub 存在的理由。

## Product Purpose

让创作者「一个视频，一键或定时发布到抖音 / 小红书 / 视频号」，无人值守、静默执行，用本机真实 Chrome 登录态降低封号风险。成功 = 任务按排期稳定跑完、各平台结果一眼可查、人工介入降到最低。

## Positioning

多平台短视频发布的桌面中枢：一个入口管理账号、素材与全部平台的发布与状态。每个平台一个账号、每台本机 Chrome 一个调试端口，账号、任务、批次在统一心智下呈现。

## Brand Personality

克制、利落、可靠。语气中性直接，不喧哗。Arc 浏览器的现代克制：干净、收边、有设计感但不表演；「工具消失进任务」。

## Anti-references

- Element Plus 默认企业后台观感——死板、模板化、一眼「管理后台」
- 花哨的 SaaS 风——玻璃拟态、渐变文字、大数字 hero、图标+标题+文本的重复卡片
- 任何让发布流程显得复杂、让状态不透明的界面

## Design Principles

1. **工具消失进任务**——信息密度为操作服务，不为装饰；界面让位给「发布、盯状态、处理异常」三件事。
2. **状态优先**——发布 / 任务 / 账号状态一眼可读；异常（人工介入、需重登、错过排期）主动浮现，不藏在层级深处。
3. **多平台统一心智**——账号、任务、结果的呈现跨平台一致，平台差异（约束、封面要求）在需要时才出现。
4. **克制即高级**——一个 accent、中性色占主体、无多余动效；空状态与加载态与主界面同级打磨。

## Accessibility & Inclusion

产品 UI 基操：正文对比 ≥ 4.5:1、键盘可完整操作、`prefers-reduced-motion` 下动效降级。具体 WCAG 级别待确认（默认 AA）。
